import React from 'react'
import * as Styled from './styled'

const Handle = (props) => {
    return(
        <>
        <Styled.HandleIcon color={props.color} />
        </>
    )
}

export default Handle