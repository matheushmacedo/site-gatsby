import React from 'react'
import * as Styled from './styled'

const Linha = (props) => {
    return(
        <>
        <Styled.LinhaIcon color={props.color}/>
        </>
    )
}

export default Linha